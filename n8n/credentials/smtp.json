{
  "id": "smtpAlerts0000001",
  "name": "SMTP (alerts)",
  "type": "smtp",
  "data": {
    "host": "={{ $env.SMTP_HOST }}",
    "port": "={{ $env.SMTP_PORT }}",
    "user": "={{ $env.SMTP_USER }}",
    "password": "={{ $env.SMTP_PASSWORD }}",
    "secure": true
  }
}
